<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta name="color-scheme" content="dark light">
<meta name="description" content="{{ description or 'Low Gravitas Blog — warm, low-blue-light themes and tools for developers.' }}">
<title>{% if title %}{{ title }} — {% endif %}Low Gravitas Blog</title>
<link rel="preload" href="https://lowgravitas.com/LowGravitasSymbols.ttf" as="font" type="font/ttf" crossorigin>
<link rel="stylesheet" href="https://lowgravitas.com/low-gravitas-zen.css">
<link rel="stylesheet" href="https://lowgravitas.com/low-gravitas-common.css">
<link rel="stylesheet" href="/css/blog.css">
<link rel="alternate" type="application/atom+xml" href="/feed.xml" title="Low Gravitas Blog">
<style>
@font-face {
  font-family: 'LowGravitasSymbols';
  src: url('https://lowgravitas.com/LowGravitasSymbols.ttf') format('truetype');
  font-display: swap;
}
</style>
</head>
<body>

<a class="skip-link" href="#main">Skip to content</a>

<nav>
  <a href="https://lowgravitas.com/" class="nav-left"><span class="brand" aria-hidden="true">&#xE900;</span> Low Gravitas</a>
  <div class="nav-right">
    <a href="/blog/">Blog</a>
    <a href="https://lowgravitas.com/low-gravitas-zen-theme/">Zen Theme</a>
    <a href="https://lowgravitas.com/low-gravitas-symbol-font/">Symbol Font</a>
    <button id="theme-toggle" aria-label="Toggle light/dark theme"></button>
  </div>
</nav>

<main id="main">
  {{ content | safe }}
</main>

<footer>&copy; 2026 Low Gravitas. <a href="https://github.com/low-gravitas">GitHub</a></footer>

<script>
(function() {
  const themeBtn = document.getElementById('theme-toggle');
  let userTheme = sessionStorage.getItem('theme') || null;
  themeBtn.textContent = String.fromCodePoint(0xF050E);
  if (userTheme) document.documentElement.setAttribute('data-theme', userTheme);

  function getTheme() {
    if (userTheme) return userTheme;
    return window.matchMedia('(prefers-color-scheme: light)').matches ? 'light' : 'dark';
  }
  function applyTheme() {
    if (userTheme) document.documentElement.setAttribute('data-theme', userTheme);
    else document.documentElement.removeAttribute('data-theme');
  }

  themeBtn.addEventListener('click', () => {
    userTheme = getTheme() === 'dark' ? 'light' : 'dark';
    sessionStorage.setItem('theme', userTheme);
    applyTheme();
  });

  window.matchMedia('(prefers-color-scheme: light)').addEventListener('change', () => {
    if (!userTheme) applyTheme();
  });
})();
</script>

</body>
</html>
